{
  "name": "mailscheduler",
  "version": "0.1.0",
  "description": "Scheduler to send recurring mails with dynamic input",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Daniel Knecht",
  "license": "MIT",
  "dependencies": {
    "node-cron": "^3.0.2",
    "dotenv": "^16.3.1",
    "nodemailer": "^6.9.5"
  }
}
